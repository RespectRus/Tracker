import Foundation

struct EditTracker {
    let tracker: Tracker
    let categoryTitle: String
    let schedule: String
    let checkCountDay: Int
    let isChecked: Bool
    let canCheck: Bool
    let indexPath: IndexPath
}
